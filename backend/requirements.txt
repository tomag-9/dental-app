Django>=5.1,<6.0
djangorestframework
django-cors-headers
psycopg2-binary
gunicorn
uvicorn
django-filter
drf-spectacular
python-json-logger
reportlab
weasyprint
Markdown
django-extensions
ipython
werkzeug
pyplusplus

ruff
black
flake8
coverage
djangorestframework-simplejwt
pyotp
qrcode[pil]
openpyxl
# Stripe billing (issues #102-#104). Optional at runtime: with STRIPE_SECRET_KEY
# unset the billing module reports itself disabled and never calls out.
stripe
# Pay by Square QR payload encoder (MIT, stdlib-only). Pinned: payment-critical
# and rarely released — upgrades must be re-verified by the round-trip tests.
pay-by-square==0.2.0
